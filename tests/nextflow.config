includeConfig '../nextflow.config'

process {
    // Limit resources to allow execution in github actions
    resourceLimits = [ cpus: 2, memory: 5.GB ]
}

// Emulate user for docker containers to avoid permission issues
docker {
    runOptions = '-u $(id -u):$(id -g)'
}
